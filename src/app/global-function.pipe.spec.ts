import { GlobalFunctionPipe } from './global-function.pipe';

describe('GlobalFunctionPipe', () => {
  it('create an instance', () => {
    const pipe = new GlobalFunctionPipe();
    expect(pipe).toBeTruthy();
  });
});
